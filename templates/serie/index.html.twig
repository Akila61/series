{% extends "layout.html.twig" %}

{% block title %}Liste des séries{% endblock %}

{% block body %}<h1>Liste de séries</h1>

    <a href="{{ path('series_new') }}">Ajouter une série</a>

    <section class="series-list">
        {% for serie in series %}
            <article>
                <img src="{{ asset('uploads/posters/series/' ~ serie.poster) }}" alt="{{ serie.name }}">
                <a href="{{ path('series_details', {'id': serie.id}) }}">
                    {{ serie.name}}({{ serie.firstAirDate | date('d/m/Y') }})</a>

            </article>
        {% endfor %}
    </section>



{% endblock %}



